{
  "name": "@harness/dedup-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing deterministic same-type vulnerability deduplication for Harness STO findings",
  "type": "module",
  "bin": {
    "dedup-mcp": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node test/run-tests.mjs",
    "inspect": "npx -y @modelcontextprotocol/inspector tsx src/cli.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
